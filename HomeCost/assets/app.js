const DB_NAME='building-ledger-db', STORE='expenses', DB_VERSION=2;
let db, records=[],pendingImport=null,activeCategory='',quickFilter='all',showAllStages=false,selectedRecordId='';
const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);
const fmt=n=>new Intl.NumberFormat('ar-SA',{maximumFractionDigits:2}).format(n||0);
const TYPE_LABELS={expense:'مصروف',refund:'مرتجع',contractPayment:'دفعة عقد',fee:'رسوم',adjustment:'تسوية'};
const STAGE_SEQUENCE=['الأرض','التصميم','العقود','البناء','العزل','اللياسة','الجبس','الكهرباء','السباكة','التكييف','الشفاطات','النوافذ','الواجهة','البلاط','الدهان','الأبواب','الستاير','السرر','النظافة','النقل'];
const SUBCATEGORY_RULES={
  'الكهرباء':[['فواتير وعدادات الكهرباء',['فاتورة كهرباء','عدادات الكهرباء']],['الكيابل',['كيبل','كيابل']],['الأسلاك',['سلك','أسلاك']],['المفاتيح والأفياش',['مفتاح','مفاتيح','فيش','أفياش']],['اللوحات والقواطع',['لوحة','طبلون','طبالين','قاطع']],['الإنارة',['إنارة','لمبة','لمبات','نجفة','فوانيس']],['الأنظمة منخفضة التيار',['كاميرات','هارديسك','سويتش','انتركوم','جهاز تسجيل','شبكة']],['المضخات والتحكم',['عوامة']],['مواد وأدوات كهربائية',['أدوات كهربائية','أغراض']],['أعمال مساندة للكهرباء',['تخريم','جبس','سلم']],['أعمال التأسيس والتركيب',['تأسيس','تمديد','تركيب','تشطيب','دفعة']]],
  'السباكة':[['المواسير والتمديدات',['ماسورة','مواسير','تمديد','جلب','سدادات']],['الأدوات الصحية',['أدوات صحية','كرسي حمام','مرحاض','شطاف','بانيو','اكسسورات الحمامات']],['الخلاطات والمغاسل',['خلاط','مغسلة','مغاسل','حوض']],['الخزانات ومستلزماتها',['خزان','خزانات','محابس']],['المضخات والغطاسات',['غطاس','عوامة']],['الصرف والصفايات',['صرف','صفاية','صفايات']],['مواد وأدوات السباكة',['أدوات سباكة']],['أعمال السباكة',['تأسيس','تركيب','تشطيبات','دفعة']]],
  'البلاط':[['الرخام والجرانيت',['رخام','جرانيت']],['السيراميك والكيشاني',['سيراميك','كيشاني','بورسلان','باركيه','تراكوتا']],['مواد التركيب',['غراء','ترويبة','اسمنت','أسمنت','رمل','بحص']],['الكرانيش والنعلات',['كرانيش','نعلات','زوايا']],['الدرج والمداخل',['درج','بسطة','مدخل']],['الأسطح والأرضيات',['بلاط السطح','أرضية','أرضيات']],['أعمال البلاط',['تبليط','بلاط','قص','دفعة','أعمال']]],
  'البناء':[['الأسمنت والخرسانة',['اسمنت','أسمنت','خرسانة']],['الحديد والتسليح',['حديد','تسليح']],['الرمل والبلوك',['رمل','بلوك']],['المقاولات والدفعات',['مقاول','دفعة']],['الإشراف والتراخيص',['إشراف','اشراف','رخصة','بلدية']],['الأعمال الإنشائية',['قواعد','أعمدة','سقف','درج']]],
  'الدهان':[['مواد الدهان',['بويه','دهان','معجون','فيتونيت']],['أعمال الدهان',['تأسيس','دفعة','أعمال']]],'اللياسة':[['مواد اللياسة',['اسمنت','أسمنت','رمل']],['أعمال اللياسة',['لياسة','دفعة','أعمال']]],
  'التكييف':[['أجهزة التكييف',['مكيف','مكيفات']],['التمديدات',['نحاس','مواسير','تمديد']],['التركيب والصيانة',['تركيب','دفعة','صيانة']]],
  'الأبواب':[['أبواب الحديد',['حديد']],['أبواب الخشب',['خشب']],['أبواب آلية',['رول','ماكينة','اتوماتيكية']],['الإكسسوارات والأقفال',['قفل','كالون','مفصلات','مفاتيح']],['التصنيع والتركيب',['تفصيل','تركيب','دفعة']]],
  'النوافذ':[['النوافذ والزجاج',['شباك','شبابيك','نافذة','زجاج']],['الألمنيوم',['المنيوم','ألمنيوم']],['التركيب',['تركيب','دفعة']]],'العزل':[['العزل المائي',['مائي','مويه']],['العزل الحراري',['حراري']],['مواد وأعمال العزل',['عزل','دفعة']]],
  'الجبس':[['مواد الجبس',['جبس','جبسمبورد','شطرطون']],['أعمال الجبس',['تركيب','دفعة','أعمال']]],'التصميم':[['التصميم المعماري',['مخطط','معماري','رخصة']],['التصميم الداخلي',['داخلي']],['الإشراف الهندسي',['إشراف','اشراف']]]
};
function normalizeText(value){return String(value||'').normalize('NFKD').replace(/[\u0300-\u036f\u0610-\u061a\u064b-\u065f\u0670\u06d6-\u06ed]/g,'').replace(/[إأآ]/g,'ا').replace(/ى/g,'ي').replace(/ة/g,'ه').replace(/ـ/g,'').replace(/\s+/g,' ').trim()}
function suggestSubcategory(description,category){const text=normalizeText(description),priority={
  'البناء':[['الإشراف والتراخيص',['اشراف','رخصه','بلديه']],['الأعمال الإنشائية',['قواعد','اعمده','سقف','درج']]],
  'البلاط':[['مواد التركيب',['غراء','ترويبه','اسمنت','رمل','بحص']],['أعمال البلاط',['دفعه اعمال','اعمال تبليط','تركيب']]],
  'الجبس':[['أعمال الجبس',['دفعه اعمال','اعمال جبس','تركيب']],['مواد الجبس',['جبس','جبسمبورد','شطرطون']]],
  'الدهان':[['أعمال الدهان',['دفعه','تاسيس بويه','اعمال دهان']],['مواد الدهان',['بويه','دهان','معجون','فيتونيت']]],
  'العزل':[['مواد وأعمال العزل',['الواح عوازل','عازل','عزل']],['العزل المائي',['مائي','مويه']],['العزل الحراري',['حراري']]],
  'النوافذ':[['واجهات الألمنيوم',['واجهه المنيوم','واجهات المنيوم']],['التركيب',['تركيب','دفعه']]]
};for(const [label,words] of priority[category]||[])if(words.some(w=>text.includes(normalizeText(w))))return label;for(const [label,words] of SUBCATEGORY_RULES[category]||[])if(words.some(w=>text.includes(normalizeText(w))))return label;const global=[['أجور وتنفيذ',['دفعه','اعمال','تركيب','تنفيذ']],['المواد والمشتريات',['شراء','مواد','عدد','كيس','كرتون']],['نقل وتوصيل',['نقل','توصيل']],['صيانة وتعديلات',['صيانه','تعديل','تعديلات']],['رسوم وخدمات',['رسوم','فاتوره','رخصه']]];for(const [label,words] of global)if(words.some(w=>text.includes(normalizeText(w))))return label;return 'بحاجة مراجعة'}
function inferCostType(description,amount){const t=normalizeText(description);if(amount<0)return 'مرتجع';if(/رسوم|رخصه|فاتوره كهرباء/.test(t))return 'رسوم وخدمات';if(/دفعه|اعمال|تركيب|تنفيذ|اشراف/.test(t))return 'أجور وتنفيذ';return 'مواد ومشتريات'}
const hijriFmt=new Intl.DateTimeFormat('en-US-u-ca-islamic-umalqura-nu-latn',{day:'2-digit',month:'2-digit',year:'numeric',timeZone:'UTC'});
function openDB(){return new Promise((resolve,reject)=>{const q=indexedDB.open(DB_NAME,DB_VERSION);q.onupgradeneeded=()=>{if(!q.result.objectStoreNames.contains(STORE))q.result.createObjectStore(STORE,{keyPath:'id'})};q.onsuccess=()=>resolve(q.result);q.onerror=()=>reject(q.error)})}
function tx(mode='readonly'){return db.transaction(STORE,mode).objectStore(STORE)}
function allDB(){return new Promise((res,rej)=>{const q=tx().getAll();q.onsuccess=()=>res(q.result);q.onerror=()=>rej(q.error)})}
function putDB(r){return new Promise((res,rej)=>{const q=tx('readwrite').put(r);q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})}
function deleteDB(id){return new Promise((res,rej)=>{const q=tx('readwrite').delete(id);q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})}
function clearDB(){return new Promise((res,rej)=>{const q=tx('readwrite').clear();q.onsuccess=()=>res();q.onerror=()=>rej(q.error)})}
function writeBatch(items,{replace=false,regenerateIds=false}={}){return new Promise((resolve,reject)=>{const transaction=db.transaction(STORE,'readwrite'),store=transaction.objectStore(STORE);if(replace)store.clear();for(const item of items){const row={...item};if(regenerateIds)row.id=crypto.randomUUID();store.put(row)}transaction.oncomplete=()=>resolve();transaction.onerror=()=>reject(transaction.error);transaction.onabort=()=>reject(transaction.error||new Error('تم إلغاء العملية'))})}
function parseHijri(v){const p=String(v).trim().replace(/[هـه]/g,'').replace(/-/g,'/').split('/').map(Number);if(p.length!==3||p.some(Number.isNaN))return null;let [d,m,y]=p;if(y<100)y+=1400;return {d,m,y}}
function hijriParts(date){const p={};hijriFmt.formatToParts(date).forEach(x=>{if(['day','month','year'].includes(x.type))p[x.type]=+x.value});return {d:p.day,m:p.month,y:p.year}}
function validISO(iso){if(!/^\d{4}-\d{2}-\d{2}$/.test(iso))return false;const d=new Date(iso+'T12:00:00Z');return !Number.isNaN(+d)&&d.toISOString().slice(0,10)===iso}
function normalizeGregorian(value){const v=String(value||'').trim();if(validISO(v))return v;const p=v.replace(/[-.]/g,'/').split('/').map(Number);if(p.length!==3||p.some(Number.isNaN))return '';const [d,m,y]=p,iso=`${String(y).padStart(4,'0')}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`;return validISO(iso)?iso:''}
function gregorianToHijri(iso){if(!validISO(iso))return '';const d=new Date(iso+'T12:00:00Z'),h=hijriParts(d);return `${String(h.d).padStart(2,'0')}/${String(h.m).padStart(2,'0')}/${h.y}`}
function hijriToGregorian(value){const t=parseHijri(value);if(!t||t.m<1||t.m>12||t.d<1||t.d>30)return '';const day=86400000,approx=t.y+579;let lo=Date.UTC(approx-2,0,1,12),hi=Date.UTC(approx+2,11,31,12);const key=x=>x.y*372+x.m*31+x.d,target=key(t);while(lo<=hi){const mid=lo+Math.floor((hi-lo)/(2*day))*day,h=hijriParts(new Date(mid)),k=key(h);if(k===target)return new Date(mid).toISOString().slice(0,10);if(k<target)lo=mid+day;else hi=mid-day}return ''}
function displayGregorian(iso){if(!iso)return '—';const [y,m,d]=iso.split('-');return `${d}/${m}/${y}`}
function dateValue(r){return r.gregorianDate||'0000-00-00'}
function showToast(msg){const t=$('#toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2600)}
function updateDateUI(){const type=$('input[name=dateType]:checked').value,inp=$('#dateInput');inp.type=type==='gregorian'?'date':'text';inp.placeholder=type==='hijri'?'مثال: 18/02/1440':'';convertPreview()}
function convertPreview(){const type=$('input[name=dateType]:checked').value,v=$('#dateInput').value;let result=type==='hijri'?hijriToGregorian(v):gregorianToHijri(v);$('#dateError').classList.add('hidden');if(!v){$('#convertedDate').textContent='أدخل التاريخ ليظهر المقابل';return}if(!result){$('#convertedDate').textContent='تعذر تحويل التاريخ';$('#dateError').textContent='تحقق من صحة التاريخ.';$('#dateError').classList.remove('hidden');return}$('#convertedDate').textContent=type==='hijri'?`${displayGregorian(result)} م`:`${result} هـ`}
async function normalizeRecord(r){r.id=String(r.id||crypto.randomUUID());r.amount=Number(r.amount);r.invoiceNumber=String(r.invoiceNumber??'');r.description=String(r.description??'').trim();r.vendor=String(r.vendor??'').trim();r.category=String(r.category??'').trim();if(r.category==='فاتورة كهرباء'){r.originalCategory=r.category;r.category='الكهرباء';r.subcategory='فواتير وعدادات الكهرباء'}r.gregorianDate=normalizeGregorian(r.gregorianDate);if(!r.gregorianDate&&r.hijriDate)r.gregorianDate=hijriToGregorian(r.hijriDate);if(!r.hijriDate&&r.gregorianDate)r.hijriDate=gregorianToHijri(r.gregorianDate);r.needsDateReview=!(r.gregorianDate&&r.hijriDate);r.transactionType=r.transactionType||(r.amount<0?'refund':'expense');const auto=suggestSubcategory(r.description,r.category);r.subcategory=r.subcategory&&!['أخرى'].includes(r.subcategory)?r.subcategory:auto;r.subcategorySource=r.subcategorySource||'automatic';r.subcategoryConfidence=Number.isFinite(Number(r.subcategoryConfidence))?Number(r.subcategoryConfidence):(r.subcategory==='بحاجة مراجعة'?0.25:0.82);r.costType=r.costType||inferCostType(r.description,r.amount);return r}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){const total=records.reduce((s,r)=>s+r.amount,0),land=records.filter(r=>r.category==='الأرض').reduce((s,r)=>s+r.amount,0),refunds=Math.abs(records.filter(r=>r.amount<0).reduce((s,r)=>s+r.amount,0)),cats=new Map();records.forEach(r=>cats.set(r.category,(cats.get(r.category)||0)+r.amount));$('#total').textContent=fmt(total);$('#count').textContent=fmt(records.length);$('#landTotal').textContent=`${fmt(land)} ر.س`;$('#constructionTotal').textContent=`${fmt(total-land)} ر.س`;$('#refundTotal').textContent=`${fmt(refunds)} ر.س`;$('#missingInvoices').textContent=fmt(records.filter(r=>!r.invoiceNumber).length);$('#reviewDates').textContent=fmt(records.filter(r=>r.needsDateReview).length);const dated=records.filter(r=>validISO(r.gregorianDate)).sort((a,b)=>dateValue(b).localeCompare(dateValue(a)));$('#lastExpense').textContent=dated.length?`آخر عملية: ${dated[0].description} — ${fmt(dated[0].amount)} ريال`:'لا توجد عمليات مؤرخة';const categories=[...cats.keys()].filter(Boolean).sort(),subcategories=[...new Set(records.map(r=>r.subcategory).filter(Boolean))].sort(),vendors=[...new Set(records.map(r=>r.vendor).filter(Boolean))].sort();$('#categoryFilter').innerHTML='<option value="">كل المراحل</option>'+categories.map(x=>`<option>${esc(x)}</option>`).join('');$('#subcategoryFilter').innerHTML='<option value="">كل البنود الفرعية</option>'+subcategories.map(x=>`<option>${esc(x)}</option>`).join('');$('#vendorFilter').innerHTML='<option value="">كل الموردين</option>'+vendors.map(x=>`<option>${esc(x)}</option>`).join('');$('#categories').innerHTML=categories.map(x=>`<option value="${esc(x)}">`).join('');$('#subcategories').innerHTML=subcategories.map(x=>`<option value="${esc(x)}">`).join('');$('#vendors').innerHTML=vendors.map(x=>`<option value="${esc(x)}">`).join('');renderTable();renderDashboard(cats)}
function renderDashboard(cats){const scope=$('input[name=stageScope]:checked')?.value||'construction',sort=$('#stageSort').value,query=normalizeText($('#stageSearch').value),land=records.filter(r=>r.category==='الأرض'),landNet=land.reduce((s,r)=>s+r.amount,0),baseRecords=scope==='construction'?records.filter(r=>r.category!=='الأرض'):records,denominator=baseRecords.reduce((s,r)=>s+r.amount,0);$('#stageDenominator').textContent=`${fmt(denominator)} ر.س`;$('#landCallout').innerHTML=`<div><span>اقتناء الأرض</span><strong>${fmt(landNet)} ر.س</strong><small>${fmt(land.length)} عملية · منفصلة عن ترتيب التنفيذ</small></div><button class="btn compact" data-action="stage-filter" data-category="الأرض">عرض القيود</button>`;let stages=[...cats].filter(([k])=>scope==='all'||k!=='الأرض').map(([name,net])=>{const rows=records.filter(r=>r.category===name),gross=rows.filter(r=>r.amount>=0).reduce((s,r)=>s+r.amount,0),refunds=Math.abs(rows.filter(r=>r.amount<0).reduce((s,r)=>s+r.amount,0));return{name,net,gross,refunds,count:rows.length,share:denominator?net/denominator*100:0}}).filter(x=>!query||normalizeText(x.name).includes(query));if(sort==='name')stages.sort((a,b)=>a.name.localeCompare(b.name,'ar'));else if(sort==='sequence')stages.sort((a,b)=>(STAGE_SEQUENCE.indexOf(a.name)<0?99:STAGE_SEQUENCE.indexOf(a.name))-(STAGE_SEQUENCE.indexOf(b.name)<0?99:STAGE_SEQUENCE.indexOf(b.name)));else stages.sort((a,b)=>b.net-a.net);const visible=showAllStages?stages:stages.slice(0,8),max=Math.max(...visible.map(x=>Math.abs(x.net)),1);$('#toggleStages').textContent=showAllStages?'عرض الأعلى فقط':`عرض الكل (${stages.length})`;$('#bars').innerHTML=visible.map((x,i)=>`<article class="stage-row ${i<3&&sort==='amount'?'top-stage':''} ${$('#categoryFilter').value===x.name?'active':''}"><button class="stage-main" data-action="stage-filter" data-category="${esc(x.name)}"><span class="stage-rank">${i+1}</span><span class="stage-info"><b>${esc(x.name)}</b><small>${fmt(x.count)} عملية${x.refunds?` · مرتجعات ${fmt(x.refunds)} ر.س`:''}</small></span><span class="stage-bar"><i style="width:${Math.abs(x.net)/max*100}%"></i></span><span class="stage-money"><b>${fmt(x.net)} ر.س</b><small>${x.share.toFixed(1)}%</small></span></button><button class="stage-analyze" data-action="category" data-category="${esc(x.name)}">تحليل</button></article>`).join('')||'<div class="empty">لا توجد مراحل مطابقة.</div>';renderOverviewCharts()}
function renderOverviewCharts(){const years=new Map();records.forEach(r=>{if(validISO(r.gregorianDate)){const y=r.gregorianDate.slice(0,4);years.set(y,(years.get(y)||0)+r.amount)}});const yr=[...years].sort(),ym=Math.max(...yr.map(x=>Math.abs(x[1])),1);$('#yearChart').innerHTML=yr.map(([y,v])=>`<button class="year-column" data-action="year" data-year="${y}" aria-label="تصفية سنة ${y}، ${fmt(v)} ريال"><b>${fmt(v)}</b><span class="column" style="height:${Math.max(4,Math.abs(v)/ym*145)}px"></span><span>${y}</span></button>`).join('')||'<div class="empty">لا توجد تواريخ كافية.</div>';const vendors=new Map();records.filter(r=>r.category!=='الأرض').forEach(r=>{if(r.vendor)vendors.set(r.vendor,(vendors.get(r.vendor)||0)+r.amount)});$('#topVendors').innerHTML=[...vendors].sort((a,b)=>b[1]-a[1]).slice(0,5).map(([n,v],i)=>`<button class="vendor-item" data-action="vendor" data-vendor="${esc(n)}"><span class="vendor-rank">${i+1}</span><span class="vendor-name">${esc(n)}</span><span class="vendor-value">${fmt(v)} ر.س</span></button>`).join('');const checks={التاريخ:records.filter(r=>!r.needsDateReview).length,المورد:records.filter(r=>r.vendor).length,التصنيف:records.filter(r=>r.category&&r.subcategory).length,المستند:records.filter(r=>r.invoiceNumber).length},mandatory=Math.round(((checks.التاريخ+checks.المورد+checks.التصنيف)/(Math.max(records.length,1)*3))*100);$('#healthScore').textContent=`${mandatory}%`;$('#healthRing').style.setProperty('--score',`${mandatory*3.6}deg`);$('#qualityList').innerHTML=Object.entries(checks).map(([k,v])=>`<div><span>${k}</span><b>${records.length?Math.round(v/records.length*100):0}%</b></div>`).join('')}
function applyDetailFilters(){const q=$('#detailSearch').value.toLowerCase(),sub=$('#detailSubcategory').value,doc=$('#detailDocument').value;const list=records.filter(r=>r.category===activeCategory&&(!q||[r.description,r.vendor,r.invoiceNumber].some(v=>String(v||'').toLowerCase().includes(q)))&&(!sub||r.subcategory===sub)&&(!doc||(doc==='with'?!!r.invoiceNumber:!r.invoiceNumber))).sort((a,b)=>dateValue(a).localeCompare(dateValue(b)));$('#categoryRows').innerHTML=list.map((r,i)=>`<tr><td>${i+1}</td><td class="dates bidi">${esc(r.hijriDate||'—')} هـ<small>${displayGregorian(r.gregorianDate)} م</small></td><td><span class="tag">${esc(r.subcategory||'بحاجة مراجعة')}</span></td><td>${esc(r.description)}</td><td>${esc(r.vendor)}</td><td class="amount">${fmt(r.amount)} ر.س</td><td><button class="btn compact" data-action="edit" data-id="${esc(r.id)}">تعديل</button></td></tr>`).join('');$('#detailMobileRecords').innerHTML=list.map(r=>`<article class="record-card"><div class="record-card-head"><div><span class="tag">${esc(r.subcategory||'بحاجة مراجعة')}</span><h3>${esc(r.description)}</h3></div><strong>${fmt(r.amount)} ر.س</strong></div><div class="record-meta"><span>${esc(r.vendor||'مورد غير محدد')}</span><span class="bidi">${esc(r.hijriDate||'—')} هـ · ${displayGregorian(r.gregorianDate)} م</span></div><button class="btn compact" data-action="edit" data-id="${esc(r.id)}">تعديل</button></article>`).join('')}
function openCategory(name){activeCategory=name;const list=records.filter(r=>r.category===name),net=list.reduce((s,r)=>s+r.amount,0),refunds=Math.abs(list.filter(r=>r.amount<0).reduce((s,r)=>s+r.amount,0)),construction=records.filter(r=>r.category!=='الأرض').reduce((s,r)=>s+r.amount,0),documentedValue=list.filter(r=>r.invoiceNumber).reduce((s,r)=>s+Math.abs(r.amount),0),absoluteValue=list.reduce((s,r)=>s+Math.abs(r.amount),0),withInvoice=list.filter(r=>r.invoiceNumber).length,vendors=new Map(),subs=new Map();list.forEach(r=>{vendors.set(r.vendor||'غير محدد',(vendors.get(r.vendor||'غير محدد')||0)+r.amount);const key=r.subcategory||'بحاجة مراجعة',obj=subs.get(key)||{value:0,count:0};obj.value+=r.amount;obj.count++;subs.set(key,obj)});$('#categoryTitle').textContent=name;$('#categoryTotal').textContent=`${fmt(net)} ر.س`;$('#categoryOperations').textContent=`${fmt(refunds)} ر.س`;$('#categoryAverage').textContent=`${construction?(net/construction*100).toFixed(1):0}%`;$('#categoryMax').textContent=`${absoluteValue?Math.round(documentedValue/absoluteValue*100):0}%`;$('#subcategoryBreakdown').innerHTML=[...subs].sort((a,b)=>b[1].value-a[1].value).map(([n,o])=>`<button class="subcat-row" data-action="subcat" data-subcategory="${esc(n)}"><span>${esc(n)} <small>${fmt(o.count)} عملية</small></span><b>${fmt(o.value)} ر.س · ${net?(o.value/net*100).toFixed(1):0}%</b></button>`).join('');$('#categoryVendors').innerHTML=[...vendors].sort((a,b)=>b[1]-a[1]).slice(0,6).map(([n,v],i)=>`<div class="vendor-item"><span class="vendor-rank">${i+1}</span><span class="vendor-name">${esc(n)}</span><span class="vendor-value">${fmt(v)} ر.س</span></div>`).join('');$('#categoryInvoices').innerHTML=`<button data-action="detail-doc" data-document="with"><strong>${fmt(withInvoice)}</strong><span>بمستند · ${fmt(documentedValue)} ر.س</span></button><button data-action="detail-doc" data-document="without"><strong>${fmt(list.length-withInvoice)}</strong><span>بلا مستند · ${fmt(absoluteValue-documentedValue)} ر.س</span></button>`;const subcats=[...new Set(list.map(r=>r.subcategory).filter(Boolean))].sort();$('#detailSubcategory').innerHTML='<option value="">كل التصنيفات الفرعية</option>'+subcats.map(x=>`<option>${esc(x)}</option>`).join('');$('#detailSearch').value='';$('#detailDocument').value='';applyDetailFilters();openDialog($('#categoryModal'))}
function closeCategoryModal(){closeDialog($('#categoryModal'))}
function openRecordDetail(id){const r=records.find(x=>x.id===id);if(!r)return;selectedRecordId=id;$('#recordTitle').textContent=r.description;$('#recordAmount').innerHTML=`<strong class="${r.amount<0?'refund':''}">${fmt(r.amount)} ر.س</strong><span class="type-badge ${r.transactionType}">${TYPE_LABELS[r.transactionType]||'مصروف'}</span>`;const details=[['التاريخ الميلادي',`${displayGregorian(r.gregorianDate)} م`],['التاريخ الهجري',`${r.hijriDate||'—'} هـ`],['المرحلة',r.category],['البند الفرعي',r.subcategory||'بحاجة مراجعة'],['المورد أو المقاول',r.vendor||'غير محدد'],['رقم المستند',r.invoiceNumber||'بلا مستند'],['طريقة الدفع',r.paymentMethod||'غير محددة'],['نوع التكلفة',r.costType||'غير محدد'],['الملاحظات',r.notes||'لا توجد']];$('#recordDetails').innerHTML=details.map(([k,v])=>`<div><span>${esc(k)}</span><b>${esc(v)}</b></div>`).join('');openDialog($('#recordModal'))}
function getFilteredRecords(){const q=normalizeText($('#search').value),cat=$('#categoryFilter').value,sub=$('#subcategoryFilter').value,vendor=$('#vendorFilter').value,type=$('#typeFilter').value,doc=$('#documentFilter').value,from=$('#dateFrom').value,to=$('#dateTo').value,sort=$('#sort').value;let list=records.filter(r=>(!cat||r.category===cat)&&(!sub||r.subcategory===sub)&&(!vendor||r.vendor===vendor)&&(!type||r.transactionType===type)&&(!doc||(doc==='with'?!!r.invoiceNumber:!r.invoiceNumber))&&(!from||r.gregorianDate>=from)&&(!to||r.gregorianDate<=to)&&(!q||[r.description,r.vendor,r.invoiceNumber,r.category,r.subcategory,r.notes,r.paymentMethod].some(v=>normalizeText(v).includes(q))));if(quickFilter==='land')list=list.filter(r=>r.category==='الأرض');if(quickFilter==='construction')list=list.filter(r=>r.category!=='الأرض');if(quickFilter==='refund')list=list.filter(r=>r.amount<0);if(quickFilter==='missingInvoice')list=list.filter(r=>!r.invoiceNumber);if(quickFilter==='reviewDate')list=list.filter(r=>r.needsDateReview);if(quickFilter.startsWith('year:'))list=list.filter(r=>r.gregorianDate.startsWith(quickFilter.slice(5)));if(quickFilter.startsWith('vendor:'))list=list.filter(r=>r.vendor===quickFilter.slice(7));return list.sort((a,b)=>sort==='amountDesc'?b.amount-a.amount:sort==='amountAsc'?a.amount-b.amount:sort==='oldest'?dateValue(a).localeCompare(dateValue(b)):dateValue(b).localeCompare(dateValue(a)))}
function activeFilterEntries(){const entries=[],map=[['categoryFilter','المرحلة'],['subcategoryFilter','البند'],['vendorFilter','المورد'],['typeFilter','النوع'],['documentFilter','المستند'],['dateFrom','من'],['dateTo','إلى']];for(const [id,label] of map){const el=$(`#${id}`);if(el.value)entries.push({id,label,value:el.options?el.options[el.selectedIndex].text:el.value})}if($('#search').value)entries.push({id:'search',label:'بحث',value:$('#search').value});if(quickFilter!=='all')entries.push({id:'quick',label:'فلتر',value:quickFilter.startsWith('year:')?`سنة ${quickFilter.slice(5)}`:quickFilter.startsWith('vendor:')?quickFilter.slice(7):{land:'الأرض',construction:'التنفيذ',refund:'المرتجعات',missingInvoice:'بلا مستند',reviewDate:'تاريخ للمراجعة'}[quickFilter]});return entries}
function renderTable(){const list=getFilteredRecords(),gross=list.filter(r=>r.amount>=0).reduce((s,r)=>s+r.amount,0),refunds=Math.abs(list.filter(r=>r.amount<0).reduce((s,r)=>s+r.amount,0)),net=gross-refunds,missing=list.filter(r=>!r.invoiceNumber).length,filters=activeFilterEntries();$('#shownCount').textContent=`عرض ${fmt(list.length)} من ${fmt(records.length)} عملية`;$('#filteredCount').textContent=fmt(list.length);$('#filteredGross').textContent=`${fmt(gross)} ر.س`;$('#filteredRefunds').textContent=`${fmt(refunds)} ر.س`;$('#filteredNet').textContent=`${fmt(net)} ر.س`;$('#filteredMissing').textContent=fmt(missing);$('#filterCount').textContent=filters.length;$('#filterChips').innerHTML=filters.map(f=>`<button class="filter-chip" data-action="remove-filter" data-filter-id="${f.id}"><span>${esc(f.label)}: ${esc(f.value)}</span> ×</button>`).join('');$('#empty').classList.toggle('hidden',list.length>0);$('#tbody').innerHTML=list.map(r=>`<tr class="ledger-row" data-action="record-detail" data-id="${esc(r.id)}"><td class="dates bidi"><b>${displayGregorian(r.gregorianDate)} م</b><small>${esc(r.hijriDate||'—')} هـ ${r.needsDateReview?'<span class="review">• يحتاج مراجعة</span>':''}</small></td><td><b>${esc(r.description)}</b><small>${esc(r.vendor||'مورد غير محدد')}</small></td><td><span class="tag">${esc(r.category)}</span><small>${esc(r.subcategory||'بحاجة مراجعة')}</small></td><td><span class="type-badge ${r.transactionType}">${TYPE_LABELS[r.transactionType]||'مصروف'}</span></td><td class="bidi">${r.invoiceNumber?esc(r.invoiceNumber):'<span class="warning-badge">بلا مستند</span>'}</td><td class="amount ${r.amount<0?'refund':''}">${fmt(r.amount)} ر.س</td><td><button class="more-btn" data-action="record-detail" data-id="${esc(r.id)}" aria-label="خيارات القيد">⋮</button></td></tr>`).join('');$('#mobileRecords').innerHTML=list.map(r=>`<article class="record-card" data-action="record-detail" data-id="${esc(r.id)}"><div class="record-card-head"><div><span class="type-badge ${r.transactionType}">${TYPE_LABELS[r.transactionType]||'مصروف'}</span><h3>${esc(r.description)}</h3></div><strong class="${r.amount<0?'refund':''}">${fmt(r.amount)} ر.س</strong></div><div class="record-meta"><span>${esc(r.category)} · ${esc(r.subcategory||'بحاجة مراجعة')}</span><span>${esc(r.vendor||'مورد غير محدد')}</span><span class="bidi">${displayGregorian(r.gregorianDate)} م · ${esc(r.hijriDate||'—')} هـ</span><span>${r.invoiceNumber?`مستند ${esc(r.invoiceNumber)}`:'⚠ بلا مستند'}</span></div><button class="more-btn" data-action="record-detail" data-id="${esc(r.id)}">عرض التفاصيل ←</button></article>`).join('')}
let lastFocus=null;function openDialog(el){lastFocus=document.activeElement;el.classList.add('open');document.querySelector('main').inert=true;setTimeout(()=>el.querySelector('button,input,select,textarea')?.focus(),20)}function closeDialog(el){el.classList.remove('open');if(!document.querySelector('.modal.open'))document.querySelector('main').inert=false;lastFocus?.focus?.()}
function openModal(){resetForm();openDialog($('#modal'))}function closeModal(){closeDialog($('#modal'))}
function resetForm(){$('#expenseForm').reset();$('#subcategory').dataset.auto='true';$('#recordId').value='';$('#formTitle').textContent='إضافة مصروف';$('#convertedDate').textContent='أدخل التاريخ ليظهر المقابل';updateDateUI()}
function editRecord(id){const r=records.find(x=>x.id===id);if(!r)return;resetForm();$('#recordId').value=r.id;$('#formTitle').textContent='تعديل المصروف';const type=r.sourceDateType==='gregorian'?'gregorian':'hijri';$(`input[name=dateType][value=${type}]`).checked=true;updateDateUI();$('#dateInput').value=type==='gregorian'?r.gregorianDate:r.hijriDate;$('#description').value=r.description;$('#vendor').value=r.vendor;$('#transactionType').value=r.transactionType||(r.amount<0?'refund':'expense');$('#amount').value=Math.abs(r.amount);$('#category').value=r.category;$('#subcategory').value=r.subcategory||suggestSubcategory(r.description,r.category);$('#subcategory').dataset.auto=r.subcategorySource==='automatic'?'true':'false';$('#invoiceNumber').value=r.invoiceNumber;$('#paymentMethod').value=r.paymentMethod||'';$('#notes').value=r.notes||'';convertPreview();openDialog($('#modal'))}
async function removeRecord(id){const r=records.find(x=>x.id===id);if(!r||!confirm(`حذف العملية: ${r.description}\nالمبلغ: ${fmt(r.amount)} ريال؟`))return;try{await deleteDB(id);records=await allDB();render();showToast('تم حذف العملية')}catch{showToast('تعذر حذف العملية')}}
async function saveForm(e){e.preventDefault();const type=$('input[name=dateType]:checked').value,raw=$('#dateInput').value,g=type==='gregorian'?normalizeGregorian(raw):hijriToGregorian(raw),h=type==='hijri'?raw:gregorianToHijri(g);if(!g||!h){showToast('تحقق من التاريخ قبل الحفظ');return}const old=records.find(r=>r.id===$('#recordId').value),description=$('#description').value.trim(),category=$('#category').value.trim(),transactionType=$('#transactionType').value,base=Math.abs(Number($('#amount').value));if(!Number.isFinite(base)){showToast('المبلغ غير صالح');return}const auto=$('#subcategory').dataset.auto==='true',r={...old,id:old?.id||crypto.randomUUID(),serial:old?.serial||records.length+1,sourceDate:raw,sourceDateType:type,hijriDate:h,gregorianDate:g,invoiceNumber:$('#invoiceNumber').value.trim(),description,vendor:$('#vendor').value.trim(),amount:transactionType==='refund'?-base:base,transactionType,category,subcategory:$('#subcategory').value.trim()||suggestSubcategory(description,category),subcategorySource:auto?'automatic':'manual',subcategoryConfidence:auto?0.72:1,paymentMethod:$('#paymentMethod').value,notes:$('#notes').value.trim(),needsDateReview:false,createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};try{await putDB(r);records=await allDB();render();closeModal();showToast(old?'تم تحديث العملية':'تمت إضافة العملية')}catch{showToast('تعذر حفظ العملية')}}
function validatePayload(payload){const incoming=Array.isArray(payload)?payload:payload?.records;if(!Array.isArray(incoming)||incoming.length>50000)throw Error('بنية الملف أو حجمه غير صالح');return incoming}
async function importFile(file){try{if(file.size>20*1024*1024)throw Error('الملف أكبر من الحد المسموح');const raw=validatePayload(JSON.parse(await file.text())),normalized=[];for(let i=0;i<raw.length;i++){const r=await normalizeRecord({...raw[i]});if(!Number.isFinite(r.amount)||!r.description||!r.category)throw Error(`السجل ${i+1} غير مكتمل`);normalized.push(r)}pendingImport={fileName:file.name,rows:normalized};$('#importSummary').innerHTML=`<strong>${esc(file.name)}</strong><span>${fmt(normalized.length)} سجل صالح للاستيراد</span><small>الاستبدال ينشئ نسخة احتياطية أولًا. الدمج يولّد معرفات جديدة لمنع الكتابة فوق السجلات الحالية.</small>`;openDialog($('#importModal'))}catch(e){showToast(e.message||'ملف الاستيراد غير صالح')}}
function downloadPayload(payload,name){const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();URL.revokeObjectURL(a.href)}
function exportData(silent=false){if(!records.length){if(!silent)showToast('لا توجد بيانات لتصديرها');return}downloadPayload({app:'مِعمار',schemaVersion:3,exportedAt:new Date().toISOString(),records},`mimar_backup_${new Date().toISOString().slice(0,10)}.json`);if(!silent)showToast('تم إنشاء النسخة الاحتياطية')}
function exportFilteredData(){const filtered=getFilteredRecords();if(!filtered.length){showToast('لا توجد نتائج لتصديرها');return}downloadPayload({app:'مِعمار',schemaVersion:3,exportedAt:new Date().toISOString(),filterSummary:activeFilterEntries(),records:filtered},`mimar_filtered_${new Date().toISOString().slice(0,10)}.json`);showToast(`تم تصدير ${fmt(filtered.length)} نتيجة`)}
async function commitImport(mode){if(!pendingImport)return;try{if(mode==='replace'&&records.length)exportData(true);await writeBatch(pendingImport.rows,{replace:mode==='replace',regenerateIds:mode==='merge'});records=await allDB();closeDialog($('#importModal'));render();showToast(`تم استيراد ${fmt(pendingImport.rows.length)} سجل بأمان`);pendingImport=null}catch{showToast('فشل الاستيراد ولم تتغير البيانات الحالية')}}
function categoryTotals(){const m=new Map();for(const r of records)m.set(r.category,(m.get(r.category)||0)+r.amount);return m}
function categoryTotals(){const m=new Map();for(const r of records)m.set(r.category,(m.get(r.category)||0)+r.amount);return m}
$('#addBtn').onclick=openModal;
$('#closeModal').onclick=$('#cancelModal').onclick=closeModal;
$('#modal').onclick=e=>{if(e.target===$('#modal'))closeModal()};
$('#closeCategory').onclick=closeCategoryModal;
$('#categoryModal').onclick=e=>{if(e.target===$('#categoryModal'))closeCategoryModal()};
$('#closeImport').onclick=$('#cancelImport').onclick=()=>{pendingImport=null;closeDialog($('#importModal'))};
$('#replaceImport').onclick=()=>commitImport('replace');
$('#mergeImport').onclick=()=>commitImport('merge');
$('#closeRecord').onclick=$('#recordClose').onclick=()=>closeDialog($('#recordModal'));
$('#recordModal').onclick=e=>{if(e.target===$('#recordModal'))closeDialog($('#recordModal'))};
$('#recordEdit').onclick=()=>{const id=selectedRecordId;closeDialog($('#recordModal'));editRecord(id)};
$('#recordDelete').onclick=async()=>{const id=selectedRecordId;closeDialog($('#recordModal'));await removeRecord(id)};
$('#expenseForm').onsubmit=saveForm;
$$('input[name=dateType]').forEach(x=>x.onchange=updateDateUI);
$('#dateInput').oninput=convertPreview;
function refreshSuggestion(){const suggested=suggestSubcategory($('#description').value,$('#category').value);if(!$('#subcategory').value||$('#subcategory').dataset.auto==='true'){$('#subcategory').value=suggested;$('#subcategory').dataset.auto='true'}}
$('#description').oninput=refreshSuggestion;
$('#category').onchange=refreshSuggestion;
$('#subcategory').oninput=()=>$('#subcategory').dataset.auto='false';
const filterIds=['search','categoryFilter','subcategoryFilter','vendorFilter','typeFilter','documentFilter','dateFrom','dateTo','sort'];
for(const id of filterIds){const el=$('#'+id);el[id==='search'?'oninput':'onchange']=()=>{renderTable();if(id==='categoryFilter')renderDashboard(categoryTotals())}}
$('#detailSearch').oninput=applyDetailFilters;
$('#detailSubcategory').onchange=applyDetailFilters;
$('#detailDocument').onchange=applyDetailFilters;
$('#toggleFilters').onclick=()=>$('#advancedFilters').classList.toggle('hidden');
$('#clearFilters').onclick=()=>{quickFilter='all';for(const id of ['search','categoryFilter','subcategoryFilter','vendorFilter','typeFilter','documentFilter','dateFrom','dateTo'])$('#'+id).value='';$('#sort').value='oldest';renderTable();renderDashboard(categoryTotals())};
$$('.stat-action').forEach(b=>b.onclick=()=>{quickFilter=b.dataset.filter;renderTable();$('#expenseLedger').scrollIntoView({behavior:'smooth'})});
$$('input[name=stageScope]').forEach(x=>x.onchange=()=>renderDashboard(categoryTotals()));
$('#stageSort').onchange=$('#stageSearch').oninput=()=>renderDashboard(categoryTotals());
$('#toggleStages').onclick=()=>{showAllStages=!showAllStages;renderDashboard(categoryTotals())};
$('#exportFiltered').onclick=exportFilteredData;
$('#importBtn').onclick=()=>$('#fileInput').click();
$('#fileInput').onchange=e=>{if(e.target.files[0])importFile(e.target.files[0]);e.target.value=''};
$('#exportBtn').onclick=()=>exportData();
$('#themeBtn').onclick=()=>{const d=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=d;localStorage.setItem('building-ledger-theme',d)};
document.addEventListener('click',e=>{
  const b=e.target.closest('[data-action]');if(!b)return;
  const a=b.dataset.action;
  if(a==='edit'){closeCategoryModal();editRecord(b.dataset.id)}
  else if(a==='delete')removeRecord(b.dataset.id);
  else if(a==='record-detail')openRecordDetail(b.dataset.id);
  else if(a==='category')openCategory(b.dataset.category);
  else if(a==='stage-filter'){$('#categoryFilter').value=b.dataset.category;quickFilter='all';renderTable();renderDashboard(categoryTotals());$('#expenseLedger').scrollIntoView({behavior:'smooth'})}
  else if(a==='year'){quickFilter='year:'+b.dataset.year;renderTable();$('#expenseLedger').scrollIntoView({behavior:'smooth'})}
  else if(a==='vendor'){quickFilter='vendor:'+b.dataset.vendor;renderTable();$('#expenseLedger').scrollIntoView({behavior:'smooth'})}
  else if(a==='subcat'){$('#detailSubcategory').value=b.dataset.subcategory;applyDetailFilters()}
  else if(a==='detail-doc'){$('#detailDocument').value=b.dataset.document;applyDetailFilters()}
  else if(a==='remove-filter'){if(b.dataset.filterId==='quick')quickFilter='all';else $('#'+b.dataset.filterId).value='';renderTable();if(b.dataset.filterId==='categoryFilter')renderDashboard(categoryTotals())}
});
document.addEventListener('keydown',e=>{const open=document.querySelector('.modal.open');if(!open)return;if(e.key==='Escape'){closeDialog(open);return}if(e.key==='Tab'){const focusable=[...open.querySelectorAll('button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled])')].filter(x=>x.offsetParent!==null||x===document.activeElement);if(!focusable.length)return;const first=focusable[0],last=focusable.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});
(async()=>{document.documentElement.dataset.theme=localStorage.getItem('building-ledger-theme')||'light';db=await openDB();records=await allDB();const migrated=[];for(const raw of records)migrated.push(await normalizeRecord(raw));if(migrated.length)await writeBatch(migrated,{replace:true});records=await allDB();render()})().catch(()=>showToast('تعذر فتح قاعدة البيانات المحلية'));
